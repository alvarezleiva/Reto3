package Reto3;

import java.time.LocalDate;
import java.time.temporal.ChronoUnit;
import java.util.Scanner;
public class funcionesG {

	public static void mismoaño(LocalDate fecha1, LocalDate fecha2) {
		
		LocalDate primerafecha;
		LocalDate segundafecha;
		
		int dias = (int) ChronoUnit.DAYS.between(fecha1,fecha2);
		int meses = (int) ChronoUnit.MONTHS.between(fecha1, fecha2);
		int años = (int) ChronoUnit.YEARS.between(fecha1, fecha2);
		
		

		
		
		
		
	}
}
